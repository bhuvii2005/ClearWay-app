spring:
  application:
    name: smart-routes-api
  datasource:
    url: jdbc:postgresql://localhost:5434/cleanroute_db
    username: postgres
    password: bhuvan#15
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

tomtom:
  api:
    key: uHuFXjmEsGuVVmVBqmDAzALCFZ92h2vY

logging:
  level:
    root: INFO
    com.cleanroute: DEBUG
